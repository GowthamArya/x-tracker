name: Deploy Angular to MonsterASP

on:
  push:
    branches:
      - main

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      # 1. Checkout repository
      - name: Checkout code
        uses: actions/checkout@v4

      # 2. Setup Node.js
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: client/package-lock.json

      # 3. Install dependencies
      - name: Install dependencies
        working-directory: client
        run: npm ci

      # 4. Build Angular application
      - name: Build Angular application
        working-directory: client
        run: npm run build -- --configuration production

      # 5. Find Angular build output
      - name: Find Angular build output
        run: |
          echo "=== Client directory ==="
          ls -la client

          echo "=== Looking for index.html ==="
          find client -type f -name "index.html" -print

      # 6. Prepare deployment files
      - name: Prepare deployment
        run: |
          rm -rf deploy
          mkdir deploy

          DIST_DIR=$(find client -type f -name "index.html" -print -quit | xargs -r dirname)

          if [ -z "$DIST_DIR" ]; then
            echo "ERROR: Angular index.html was not found."
            echo "Build output:"
            find client -maxdepth 5 -type f | head -100
            exit 1
          fi

          echo "Angular build directory: $DIST_DIR"

          cp -R "$DIST_DIR"/. deploy/

          echo "=== Deployment files ==="
          find deploy -maxdepth 2 -type f

      # 7. Deploy to MonsterASP
      - name: Deploy to MonsterASP
        uses: SamKirkland/FTP-Deploy-Action@v4.3.6
        with:
          server: ${{ secrets.FTP_SERVER }}
          username: ${{ secrets.FTP_USERNAME }}
          password: ${{ secrets.FTP_PASSWORD }}
          server-dir: /wwwroot/
          local-dir: ./deploy/